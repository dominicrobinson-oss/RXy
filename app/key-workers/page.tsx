import StaticPlaceholderPage from "@/app/components/StaticPlaceholderPage";

export default function KeyWorkersPage() {
  return (
    <StaticPlaceholderPage
      heading="Key Workers"
      description="This is a placeholder Key Workers page built to match the Myprotein layout contract while key worker reward content is prepared."
      eyebrow="Loyalty & Rewards"
    />
  );
}