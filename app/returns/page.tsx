import StaticPlaceholderPage from "@/app/components/StaticPlaceholderPage";

export default function ReturnsPage() {
  return (
    <StaticPlaceholderPage
      heading="Returns"
      description="This is a placeholder Returns page built to match the Myprotein layout contract while policy content is prepared."
      eyebrow="Help & Information"
    />
  );
}